### To Do Improvment & Fixes ###
+ All passwords which are in app.props&yamls should be hashed with jasypt ency.
+ Request & Responses should be logged at apigw with reactive-programing
+ A Log server is a neccessity, it collect all logs from other services.
+ A Config server should be implemented(SpringCloudConfig).
+ A kibana or ELK tool should add as docker cont or else.
+ A cluster consists from three kafka sample and a zookeper should be implemented.
